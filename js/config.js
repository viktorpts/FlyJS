const SIMULATION_INTERVAL = 15;
const SERVER_INTERVAL = 50;
const CLIENT_INTERVAL = 25;
const SCREEN_WIDTH = 800;
const SCREEN_HEIGHT = 600;

export default {
    SIMULATION_INTERVAL,    // Time between physics updates, Milliseconds
    SERVER_INTERVAL,        // Time between server updates, Milliseconds
    CLIENT_INTERVAL,        // Time between command packets, Milliseconds
    SCREEN_WIDTH,
    SCREEN_HEIGHT
}